# =====================================================
# SPOTFINDER - SPRING BOOT CONFIGURATION
# =====================================================

# --- SERVER CONFIGURATION ---
server.port=8080
spring.application.name=SpotFinder Platform

# =====================================================
# DATABASE (MySQL)
# =====================================================
spring.datasource.url=jdbc:mysql://spotfinderultramaxpro.mysql.database.azure.com:3306/spotfinder_db?useSSL=true&requireSSL=true&serverTimezone=UTC
spring.datasource.username=patata
spring.datasource.password=Patata123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =====================================================
# JPA / HIBERNATE CONFIGURATION
# =====================================================
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=com.spotfinder.backend.v1.shared.infrastructure.persistence.jpa.SnakeCasePhysicalNamingStrategy

# =====================================================
# JWT SECURITY CONFIGURATION
# =====================================================
# Secret key for signing JWT tokens (must be long and random)
authorization.jwt.secret=8Zz5tw0Ionm3XPZZfN0NOml3z9FMfmpgXwovR9fp6ryDIoGRM8EPHAB6iHsc0fb
# Token validity period in days
authorization.jwt.expiration.days=30

# =====================================================
# SWAGGER / DOCUMENTATION METADATA
# =====================================================
documentation.application.description=@project.description@
documentation.application.version=@project.version@

# =====================================================
# MQTT BROKER CONFIGURATION
# =====================================================
mqtt.enabled=true
mqtt.broker.url=ecf8382af95c43c299c63906bb1b5220.s1.eu.hivemq.cloud
mqtt.broker.port=8883
mqtt.broker.username=spotfinder
mqtt.broker.password=spotfinder2025

# =====================================================
# CORS CONFIGURATION (en WebSecurityConfiguration.java)
# -----------------------------------------------------
# Asegúrate de permitir http://localhost:4200 (Angular) en tu configuración CORS:
# corsConfiguration.setAllowedOrigins(List.of("http://localhost:4200"));
# corsConfiguration.setAllowedMethods(List.of("GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"));
# corsConfiguration.setAllowedHeaders(List.of("Authorization", "Content-Type"));
# =====================================================

# =====================================================
# SPRING DEVTOOLS (para autorestart local)
# =====================================================
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# =====================================================
# LOGGING CONFIGURATION
# =====================================================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.web=INFO
logging.level.com.spotfinder.backend=DEBUG
